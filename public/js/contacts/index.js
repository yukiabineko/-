window.addEventListener('load',()=>{
    /**
     * 問い合わせリスト
     */
    document.querySelectorAll('.contact-list').forEach( contact =>{
        contact.addEventListener('click', event =>{
            //問い合わせ内容コンテンツ
            let textContent = contact.children[0].children.length;
            
        });
    });
});